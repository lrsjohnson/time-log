DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.sqlite3',
        'NAME': 'db.dat',
        'PORT': '',                      # Set to empty string for default. Not used with sqlite3.
    }
}
